After knitting and rendering `dataPrep_Monitor.Rmd`, overwrite this file with a symlink to `dataPrep_Monitor.md`.

E.g., on Linux/macOS from terminal:

```bash
cd C:/Users/michelet/Documents/GitHub/dataPrep_Monitor
rm README.md && ln -s dataPrep_Monitor.md README.md
```
